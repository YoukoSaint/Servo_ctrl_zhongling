# 众灵舵机通信协议（ZL Servo Protocol）

> **来源**：`众灵舵机使用手册-250508.pdf`（第 5 章 "舵机控制指令"）
> **适用型号**：众灵 ZP 系列总线舵机（兼容 PM 系列 PWM 舵机的指令子集）
> **物理层**：UART / 半双工 TTL，115200 bps 默认（可调 9600 / 19200 / 38400 / 57600 / 115200 / 128000 / 256000 / 1000000）
> **编码**：8N1，ASCII 文本指令
> **地址范围**：0–254（`255` 为广播地址，仅对写指令有效）

---

## 1. 帧结构

所有指令遵循同一文本协议，**固定以 `#` 开头，以 `!` 结尾**，所有数字字段采用**左侧补零**的定长编码。

### 1.1 通用格式

```
#  AAA  [PWM]  T  [TIME]  !
│  │    │      │  │        │
│  │    │      │  │        └─ 帧尾 0x21
│  │    │      │  └─ 4 位运行时间（ms），范围 0000–9999
│  │    │      └─ 时间字段分隔符 'T'
│  │    └─ 4 位 PWM 脉宽，范围 0500–2500（单位与舵机中位相关）
│  └─ 3 位 ID，范围 000–254（255 = 广播）
└─ 帧头 0x23
```

> **特殊说明**：
> - 所有数值字段宽度固定，不足位补 `0`。
> - 中位 PWM 默认为 `1500`（对应 0° 位置）。
> - PWM `500` ↔ `-90°`（270° 模式） / `-90°`（180° 模式）
> - PWM `2500` ↔ `+90°`（270° 模式） / `+90°`（180° 模式）
> - PWM `1500` ↔ 中位 0°

### 1.2 群控语法（同步多舵机）

控制 2 个或更多舵机时，整组指令需用 `{}` 包裹，前缀 `{G` 表示群发。

```
{G#000P1602T1000!#001P2500T0000!#002P1500T1000!}
```

各舵机在同一时刻启动动作，时间参数 `T` 控制各自的运动时长。

---

## 2. 指令集

> 说明：以下 `000` 是占位 ID，请按实际舵机 ID 替换。
> 所有**写入**指令成功返回 `#OK!`，失败无返回。
> 所有**读取**指令返回 `#000Pxxxx!` 格式的应答。

### 2.1 运动控制

| # | 指令 | 名称 | 说明 | 返回 |
|---|------|------|------|------|
| 1 | `#000P1500T1000!` | 写位置 + 时间 | 立即以指定时间（ms）平滑转动到目标 PWM | `#OK!` |
| 2 | `#000PULK!` | 释放扭矩（掉电） | 舵机失去保持力，可手动拨动；用于手动调零/解除抱死 | `#OK!` |
| 3 | `#000PULR!` | 恢复扭矩 | 重新进入位置保持 | `#OK!` |

> **运动时间 `T`**：
> - `T > 0`：在指定毫秒内完成运动。
> - `T = 0`：**以最快速度**运动到目标位置（无平滑）。
> - `T = 9999`：最长单次时间。

### 2.2 状态查询

| # | 指令 | 名称 | 说明 | 返回 |
|---|------|------|------|------|
| 4 | `#000PVER!` | 读取固件版本 | 固件版本号 | `#000PV0.97!` |
| 5 | `#000PID!` | 读取当前 ID | 校验总线上的舵机 ID | `#000P!` / `#005P!` 等 |
| 6 | `#000PRAD!` | 读取当前位置（PWM） | 当前 PWM 值 | `#000P1500!` |
| 7 | `#000PMOD!` | 读取工作模式 | 1–8 见 §2.3 | `#000PMOD1!` 等 |

### 2.3 模式设置

| # | 指令 | 名称 | 说明 | 返回 |
|---|------|------|------|------|
| 8 | `#000PMOD1!` | 设模式 1 | 270° 舵机，**顺时针**正方向 | `#OK!` |
|   | `#000PMOD2!` | 设模式 2 | 270° 舵机，**逆时针**正方向 | `#OK!` |
|   | `#000PMOD3!` | 设模式 3 | 180° 舵机，顺时针 | `#OK!` |
|   | `#000PMOD4!` | 设模式 4 | 180° 舵机，逆时针 | `#OK!` |
|   | `#000PMOD5!` | 设模式 5 | 360° **多圈**模式，顺时针 | `#OK!` |
|   | `#000PMOD6!` | 设模式 6 | 360° 多圈模式，逆时针 | `#OK!` |
|   | `#000PMOD7!` | 设模式 7 | 360° **定时**模式，顺时针 | `#OK!` |
|   | `#000PMOD8!` | 设模式 8 | 360° 定时模式，逆时针 | `#OK!` |

> **多圈/定时模式说明**：
> - 模式 5/6（多圈）：`#000P1800T1000!` 表示以 `300/s` 速度**转 1000 圈**后停止。`T=0000` 表示以 `300/s` **无限循环**。
> - 模式 7/8（定时）：`#000P1800T1000!` 表示以 `300/s` 速度**转动 1000 秒**后停止。`T=0000` 表示以 `300/s` **无限循环**。
> - 圈/秒的换算由当前 PWM 与中位 `1500` 的差值 `ΔPWM` 决定（差越大越快，差值 300 对应 300 步/秒）。

### 2.4 运动控制（扩展）

| # | 指令 | 名称 | 说明 | 返回 |
|---|------|------|------|------|
| 9  | `#000PDPT!` | 暂停 | 停止当前运动；后续指令**仍可继续**接收并在当前位置恢复 | `#OK!` |
| 10 | `#000PDCT!` | 取消暂停 | 解除暂停状态，继续未完成的运动 | `#OK!` |
| 11 | `#000PDST!` | 停止 | 立即停在当前位置；**后续指令不再执行** | `#OK!` |

### 2.5 总线与系统

| # | 指令 | 名称 | 说明 | 返回 |
|---|------|------|------|------|
| 12 | `#000PBD0!` | 设波特率 | `0=9600` `1=19200` `2=38400` `3=57600` `4=115200`（默认）`5=128000` `6=256000` `7=1000000` | 成功时返回新波特率，如 `#000PBD9600!` |
| 13 | `#000PSCK!` | 校准中位 | 将当前位置设为中位（1500） | `#OK!` |
| 14 | `#000PCSD!` | 设中位 | 设置自定义中位 PWM（`0500`–`2500`），默认 `1500` | `#OK!` |
| 15 | `#000PCSM!` | 去使能中位 | 取消自定义中位，恢复默认 | `#OK!` |
| 16 | `#000PCSR!` | 恢复中位 | 重新启用 `#000PCSD` 设置的中位 | `#OK!` |

### 2.6 范围与 ID

| # | 指令 | 名称 | 说明 | 返回 |
|---|------|------|------|------|
| 17 | `#000PSMI!` | 设最小值 | 最小 PWM，默认 `0500`；**写入后下次上电生效** | `#OK!` |
| 18 | `#000PSMX!` | 设最大值 | 最大 PWM，默认 `2500`；**写入后下次上电生效** | `#OK!` |
| 19 | `#000PID001!` | 修改 ID | 把当前舵机 ID 改为 `001`（**0–254**，**不能改为 255**） | 成功后返回 `#001P!` |
| 20 | `#000PCLEO!` | 复位部分参数 | 恢复：模式默认 `1`，波特率默认 `115200`，中位 `1500` | `#OK!` |
| 21 | `#000PCLE!`  | 全部复位 | 同上 + ID 恢复为 `000` | `#OK!` |

---

## 3. 应答格式

| 类型 | 格式 | 例子 |
|------|------|------|
| 通用成功 | `#OK!` | `#OK!` |
| 读 PWM | `#AAApxxxx!` | `#005P1500!`（ID=5，当前 PWM=1500） |
| 读模式 | `#AAAPMODn!` | `#005PMOD3!`（ID=5，模式 3） |
| 读版本 | `#AAAPVx.xx!` | `#000PV0.97!` |
| 读 ID | `#AAAP!` | `#005P!` |
| 读波特率 | `#AAAPBDxxxxx!` | `#000PBD115200!` |
| 修改 ID 成功 | `#BBBP!` | 把 ID 从 0 改为 5 后返回 `#005P!` |

- 应答中 ID 是**应答方**的 ID（不是询问方）。
- 写入失败时**无应答**（需上位机超时检测）。
- 广播地址 `255` **不会**产生应答。

---

## 4. 编程参考（上位机协议栈实现要点）

### 4.1 发送器

```python
def write_position(id_: int, pwm: int, time_ms: int) -> str:
    assert 0 <= id_ <= 254 or id_ == 255   # 255 = 广播
    assert 500 <= pwm <= 2500
    assert 0 <= time_ms <= 9999
    return f"#{id_:03d}P{pwm:04d}T{time_ms:04d}!"
```

### 4.2 接收器

- 帧分隔符：开始 `#`，结束 `!`。
- 长度：最短 `#P!`（读取应答无参数时）、最长约 13 字节（写指令），含群控时需按 `{G...}` 处理嵌套。
- 收到不完整帧时**保留**到下一次接收，跨包重组。

### 4.3 群控帧

- 以 `{G` 起始，`}` 终止。
- 内部嵌入多个 `#…!` 帧，**共用**串口。
- 上位机解析时按 `{` `}` 切分后逐条处理。
- **广播（ID=255）** 与群控可混用，但群控优先级更高（多 ID 时用群控，广播用于无差别指令如紧急停止）。

### 4.4 错误检测

- **超时**：写指令若 `50ms` 内无 `#OK!` 应答，标记失败、重试（最多 3 次）。
- **校验**：收到 `#…!` 时验证 `id`、`pwm`、`time` 字段长度是否符合协议。
- **总线空闲**：群发结束后等待 `20ms` 再发下一组，避免总线冲突。

---

## 5. PWM ↔ 角度换算

众灵舵机手册未提供 PWM→° 的固定公式，但通过**模式与中位**可以推算：

| 模式 | 角度范围 | PWM 范围 | 公式 |
|------|----------|----------|------|
| 1/2 (270°) | -135° ~ +135° | 500 ~ 2500 | `angle = (pwm - 1500) / 1000 * 135` |
| 3/4 (180°) | -90° ~ +90°   | 500 ~ 2500 | `angle = (pwm - 1500) / 1000 *  90` |
| 5/6/7/8 (360°)| 多圈/定时 | — | 不对应角度，按速度 PWM 差值 |

> 实际使用中，**上位机建议**直接使用 PWM 1000 / 1500 / 2000 / 2500 这 4 个常用值进行控制，避免依赖厂商未公开的换算公式。

---

## 6. 循环控制模式（上位机需求映射）

上位机需要提供 **循环控制**：起始角度 → 终止角度，循环 N 次，周期 T（单程 ms）。

| 上位机输入 | 协议映射 |
|------------|----------|
| 起始角度 `a0` | PWM `p0 = round(1500 + a0 / 135 * 1000)`  （以 270° 模式为例） |
| 终止角度 `a1` | PWM `p1 = round(1500 + a1 / 135 * 1000)` |
| 周期时长 `T` (ms) | 写入指令的时间字段 `T`（PWM/T） |
| 循环次数 `N` | 程序内循环 N/2 次**来回**，或 N 次单程 |

**伪代码**：

```python
for i in range(N // 2):
    send(f"#{id:03d}P{p0:04d}T{T:04d}!")   # 起点 → 终点
    wait_for_ok(timeout=50)
    send(f"#{id:03d}P{p1:04d}T{T:04d}!")   # 终点 → 起点
    wait_for_ok(timeout=50)
```

N 为奇数时，最后一次只走单程（到达目标角度即可）。

---

## 7. 协议局限与上位机适配建议

1. **协议为查询式**：每次状态需要主动发送读取指令，没有周期性上报机制。
2. **无速度反馈**：协议没有速度/扭矩/温度的读取指令（手册未列），只有位置。
3. **广播无应答**：紧急停机（`#255PDST!`）无法确认是否所有舵机收到，建议群控兜底。
4. **中位漂移**：长时间使用后中位可能漂移，提供**手动 `#000PSCK!` 校准**按钮。
5. **写指令无失败应答**：超时即视为失败，UI 需要给用户**明确反馈**。
